# Base URL of the backend API (used once auth/portal pages are wired to a real backend)
VITE_API_BASE_URL=http://localhost:4000/api

# Public app name, shown in a few places in the UI
VITE_APP_NAME=UpEducative
